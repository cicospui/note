"""
# 普通操作
len()           求元素个数
max()           容器内的最大元素
min()           容器内的最小元素
# 转换操作
list(容器)      容器转换成列表
str(容器)       容器转换成字符串
tuple(容器)     容器转换成元组
set(容器)       容器转换成集合
# 通用排序功能
sorted(容器,[reverse = True])
排序结果均会变为列表,若需反转传入True
# 字符串排序规则
根据ASCII码排序
"""